CHAMELEON_API_BASE_URL=http://127.0.0.1:19200
# Existing Command Center V2 synthetic read-only reference; never a provider write path.
CHAMELEON_COMMAND_CENTER_URL=http://127.0.0.1:19100
# The BFF derives identity from the HttpOnly session cookie; it does not accept
# browser-supplied role or user headers.
NEXT_PUBLIC_TWENTY_URL=http://127.0.0.1:19300/
NEXT_PUBLIC_WOO_URL=http://127.0.0.1:19400/
# Configure only a supported Chatwoot/reference URL. Do not use legacy 19210; it is not Chatwoot.
# NEXT_PUBLIC_CHATWOOT_URL=
